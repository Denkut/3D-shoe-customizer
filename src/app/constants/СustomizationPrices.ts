import { ShoePart } from "./ShoeParts";

export const CUSTOMIZATION_PRICES: Record<ShoePart, number> = {
  laces: 150,
  mesh: 250,
  caps: 200,
  inner: 180,
  sole: 300,
  stripes: 220,
  band: 160,
  patch: 140,
};
